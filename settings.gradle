include 'obi:sample-12c'

if (file('../checkmate').exists()) {
  includeBuild('../checkmate') {
    dependencySubstitution {
      substitute module('gradle.plugin.com.redpillanalytics:checkmate') with project(':')
    }
  }
}